package markov_matrix;

public interface InterfaceMarkovMatrix {
	
	public boolean numberControl(double matrix[][]);

	public boolean valControl(double matrix[][]);

	public boolean validMatrix(double matrix[][]);
}
